To run this program, simply place PDF resumes in the a folder under the root directory titled "resumes" and then run the commands
```
pipenv install
pipenv run
```

If pipenv is not install, run
```
pip install pipenv
```